# yaml-language-server: $schema=https://coderabbit.ai/integrations/schema.v2.json
inheritance: true
reviews:
  # Post the high-level summary in CodeRabbit's walkthrough comment instead of
  # rewriting the (author-written) PR description. Explicit repo-level setting
  # overrides the org default deterministically.
  high_level_summary_in_walkthrough: true
